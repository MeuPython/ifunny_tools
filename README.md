basta executar o app: app-not-root.exe

Importante!: O arquivo credentials.json é importante para o funcionamento do aplicativo.

app-not-root.exe, credentials.json e token.json devem ficar na mesma pasta.